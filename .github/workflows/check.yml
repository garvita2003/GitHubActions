name: Manual Matrix Workflow

on:
  workflow_dispatch:
    inputs:
      selected_os:
        description: 'Target OS (ubuntu-latest, windows-latest, macos-latest, or all)'
        required: false
        default: 'all'

jobs:
  run-on-os:
    strategy:
      matrix:
        os: ${{ fromJson(github.event.inputs.box-agent != 'all' && github.event.inputs.box-agent != '' && format('["{0}"]', github.event.inputs.box-agent) || '["ubuntu-latest", "windows-latest", "macos-latest"]') }}
        
    runs-on: ${{ matrix.os }}

    steps:
      - name: Echo OS
        run: echo "Running on ${{ matrix.os }}"
